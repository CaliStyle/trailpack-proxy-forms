'use strict'
/* global describe, it */
const assert = require('assert')

describe('FormController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['FormController'])
  })
})
